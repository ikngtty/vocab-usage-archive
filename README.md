# Vocab Usage Archive

A web service for language learning.

This service is available for allowed users only.\
To allow a user, you should set the custom claim `available` for the user.
(Use [/scripts/custom-claim/](/scripts/custom-claim/) .)
