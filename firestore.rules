rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    match /users/{uid}/vocabs/{word} {
      allow read, write: if request.auth != null
        && request.auth.uid == uid
        && request.auth.token.available == true;
    }
  }
}
